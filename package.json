{
  "name": "template",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "start": "vite --open --host",
    "build": "vite build",
    "test": "eslint src",
    "lint": "eslint --fix src"
  },
  "dependencies": {
    "image-pixels": "^2.2.2",
    "playpass": "^1.2.1",
    "stackblur-canvas": "^2.5.0"
  },
  "devDependencies": {
    "eslint": "^8.11.0",
    "vite": "^2.8.0"
  }
}
